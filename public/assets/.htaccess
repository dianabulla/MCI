# CSS/JS con ?v= en la URL: caché larga; al desplegar, actualizar public/assets/.build
<IfModule mod_headers.c>
  <FilesMatch "\.(css|js)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
</IfModule>
